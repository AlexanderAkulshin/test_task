apiVersion: apps/v1
kind: Deployment
metadata:
  name: not-used
spec:
  template:
    spec:
      containers:
        - name: app
          env:
            - name: PROD
              value: "true"
          resources:
            limits:
              cpu: "1000m"
              memory: "512Mi"